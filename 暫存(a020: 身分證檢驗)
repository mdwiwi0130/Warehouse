#include <iostream>

using namespace std;

int main()
{
    char a;
    int z,x;
    cin>>a>>z;
    x=(int)a;
    if(x==73)
        x=34;
    else if(64<x&&x<72)
        x=x-55;
    else if(73<x&&x<79)
        x=x-56;
    else if(x==79)
        x=35;
    else if(79<x&&x<86)
        x=x-57;
    else if(x==87)
        x=32;
    else if(87<x&&x<91)
        x=x-58;
    x=x/10+x%10*9+z%100000000;
    z=z/10;
    for(int c=1;c<=8;++c){
        x=x+z%10*c;
        z=z/10;
    }
    if(x%10==0)
        cout<<"real\n";
    else
         cout<<"fake\n";
    return 0;
}
